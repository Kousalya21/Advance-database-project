Python version used: Python 3.7

import the csv file all_months.csv in your IBM database name the table as 'quakes'

In azuredb.py:
1. replace the connection variables with your own Azure database connection variables.

In azureredis.py:
1. replace the connection variables with your own Azure Redis connection variables.

To run locally, run the following commands:

pip install -r requirements
python application.py